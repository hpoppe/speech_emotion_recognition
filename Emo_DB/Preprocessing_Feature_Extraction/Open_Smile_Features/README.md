# OpenSmile Features

## Background

This directory contains experimental work on extracting eGeMAPS features from the Emo-DB dataset using OpenSmile. The initial approach involved creating separate training and test datasets, with the training set further split into training and validation subsets during the training process.